#include <stdlib.h>
#include "main.h"

/**
 * _abs - Function for absulute value
 * @u: Parameter for the function
 *
 * Return: Return the abs value of r
 */
int _abs(int u)
{
	int value;

	value = abs(u);
	return (value);
}
